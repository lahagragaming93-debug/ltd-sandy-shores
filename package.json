{
  "name": "ltd-sandy-shores-build",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Build front LTD Sandy Shores : bundle+minifie chaque page (esbuild). Firebase reste externe (CDN).",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
